import 'package:flutter/material.dart';

class BallDetailSelectorSection extends StatelessWidget {
  const BallDetailSelectorSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
